package classwork_7_4;

public class Runner {
	double min = 9999;
	double max = 0;
	for(double x1, double y1, double x2, double y2
			double dx = x2 - x1;
			double dy = y2 - y1;	
) 
{
		double dist = sumras(0, 0, x, y);
		if(dist < min) min = dist;
		if(dist > max) max = dist;
		System.out.println( Math.sqrt(dx*dx + dy*dy));
		}
}
